import { config } from '@vue/test-utils'

// Here you can disable heavy components or replace global dependencies
config.global.stubs = {}

// If you use plugins, you can connect them here
// config.global.plugins = [myPlugin]
